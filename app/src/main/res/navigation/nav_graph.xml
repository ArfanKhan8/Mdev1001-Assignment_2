<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home_page"
    >

    <fragment
        android:id="@+id/home_page"
        android:name="com.study.room.pages.HomePage"
        android:label="HomePage"
        tools:layout="@layout/fragment_home_page">
        <action
            android:id="@+id/action_home_page_to_edit_movie_page"
            app:destination="@+id/edit_movie_page"/>
    </fragment>
    <fragment
        android:id="@+id/edit_movie_page"
        android:name="com.study.room.pages.EditMoviePage"
        android:label="Edit Movie"
        tools:layout="@layout/fragment_edit_movie_page" >

    </fragment>
</navigation>